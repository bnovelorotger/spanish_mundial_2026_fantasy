# components/layout

App shell, navigation and structural components shared across pages.

Expected files (created in later phases):

- `AppShell.tsx` — Top-level wrapper for the protected app (Phase 4).
- `Header.tsx` — Top header with profile/logout (Phase 4).
- `BottomNav.tsx` — Fixed mobile bottom navigation (Phase 4).

See [docs/TECHNICAL_ARCHITECTURE.md](../../docs/TECHNICAL_ARCHITECTURE.md)
and [docs/IMPLEMENTATION_PLAN.md](../../docs/IMPLEMENTATION_PLAN.md) Phase 4.

Visual: every component in this folder must follow the Private Tournament
Mode identity defined in [docs/BRANDBOOK.md](../../docs/BRANDBOOK.md) —
especially §11.1 (dashboard order) and §15 (mobile layout + bottom nav).
